# Base URL for the API (keep as /api to use Vite dev proxy)
VITE_API_BASE=/api

# API key for backend authentication (required)
VITE_API_KEY=your_api_key_here

# Backend target for Vite dev proxy (default: http://localhost:3000)
VITE_API_TARGET=http://localhost:3000
